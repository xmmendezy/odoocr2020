odoo.define('custom_studio.CalendarEditor', function (require) {
"use strict";

var CalendarRenderer = require('web.CalendarRenderer');

var EditorMixin = require('custom_studio.EditorMixin');

return CalendarRenderer.extend(EditorMixin, {
});

});
